# ToolBar-MVVM-DataBinding
Idea is to set MainActivity Toolbar Title change to the corresponding fragment,which are being loaded. 
Also the make the toolbar navigation back function work on all fragments
